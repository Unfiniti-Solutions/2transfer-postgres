# 2transfer-postgres

PostgreSQL 16 custom image with:
- **pgvector** 0.8.x — vector similarity search
- **PostGIS** 3.x — geospatial queries

Built on `pgvector/pgvector:pg16`.

Used by api.2transfer.travel on Railway.

